let petsAnimal : string [ ] = [ `Dog`, `Cat`, `Snake` ];
for ( let i=0 ; i<petsAnimal.length ; i++ ) {
    console.log(`${petsAnimal[i]}`);
}
for ( let i=0 ; i<petsAnimal.length ; i++ ) {
    console.log(`A ${petsAnimal[i]} would make a great pet.`);
}
console.log(`These animals would make a great pet!`);